import 'package:flutter/material.dart';

const kPrimeCol = Color(0xFFFF7643);
const kPrimeLightCol = Color(0xFFFFECDF);
const kPrimeGradCol = LinearGradient(
  begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [Color(0xFFFFA53E), Color(0xFFFF7643)],
    );
const kSecCol = Color(0xFF979797);
const kTextCol = Color(0xFF757575);

const kAnimDuration = Duration(milliseconds: 200);